/*
Package setup holds the core variables and control variables of the monitor.

It includes the funciton for database creation and config file ceation if required.
All the control variables storing the status of the modules are initialised from the
status file. Package also contains function to update the config, setup and db files
to drive whenever required.
*/
package setup
