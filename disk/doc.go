/*

Package disk scans the partitions for the free disk blocks and the inodes.

The package includes actions required to clean the space if required. The module \
implements a driver function which can be controlled using the Config struct. The
parameters are extracted from the database.

*/
package disk
